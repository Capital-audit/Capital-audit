<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/cell_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Background color for cells -->
            <stroke android:color="@color/black" android:width="1dp" /> <!-- Border color and width -->
        </shape>
    </item>

    <!-- Padding -->
    <item android:left="1dp" android:right="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Background color for cells -->
        </shape>
    </item>
</layer-list>
