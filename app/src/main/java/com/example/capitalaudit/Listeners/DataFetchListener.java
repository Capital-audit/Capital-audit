package com.example.capitalaudit.Listeners;

/**
 * This interface is used to notify the activity that the data has been fetched.
 * This is used to update the UI when the data has been fetched.
 */
public interface DataFetchListener
{
    void onDataFetched();
}

